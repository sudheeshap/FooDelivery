import DATA_MENU_GROUPS from '../data/menu-groups';
/**
 * Returns menu groups  for restaurant
 */
export default function getDataMenuGroups() {
  const products = DATA_MENU_GROUPS;

  return products;
}
